package id.widiarifki.compose.data

import id.widiarifki.compose.BuildConfig

object Constant {
    const val DB_NAME = "${BuildConfig.APPLICATION_ID}_db"
    const val DB_VERSION = 1
    const val TBL_SHOPPING_ITEM = "shopping_item"
}